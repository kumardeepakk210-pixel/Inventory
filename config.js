// WishRite Inventory — Public Frontend Configuration
// NOTE: Database credentials, keys, and private Supabase configuration are strictly
// maintained server-side in environment variables and must NEVER be stored here.
window.WISHRITE_CONFIG = {
    apiBaseUrl: "/api"
};
