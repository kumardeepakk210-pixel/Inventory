// ==============================================================================
// WISHRITE INVENTORY — VERCEL CATCH-ALL SERVERLESS ENTRYPOINT
// Natively catches all /api/* requests and dispatches them to Express.
// ==============================================================================

import handler from './index.js';

export default handler;
